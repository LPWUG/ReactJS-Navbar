import React from 'react'

export default function Button({textColor, color}) {
  return (
    <button className={`bg-[${color}] text-[${textColor}}] py-2.5 px-4.5 rounded-13px`}>
      
    </button>
  )
}
